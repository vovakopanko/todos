export enum ROUTES {
  LOGIN = 'LoginScreen',
  REGISTER = 'RegisterScreen',
  SIGN_UP = 'SignUp',

  HOME = 'HomeScreen',
  ACCOUNT = 'AccountScreen',
}

export enum MAIN_ROUTES {
  TAB_BAR = 'TabBar',
}

export enum TABS {
  HOME = 'HomeTab',
  ACCOUNT = 'AccountTab',
}

export const bottomlessScreens: (string | undefined)[] = [];
export const headerlessScreens: (string | undefined)[] = [];
